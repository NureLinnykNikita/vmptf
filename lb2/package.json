{
  "name": "mini-social-network-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Laboratory project: Node.js Express + React social network with posts, comments, friends, messages, search and gzip compression.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "start": "npm run start --workspace server",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
